<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for exact reminder delivery on Android 12+; without this permission,
         AlarmManager may downgrade reminders to inexact timing. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAdvancedSchedule"
        tools:targetApi="31">

        <!-- Launcher keeps entry flow centralized: WelcomeActivity can decide whether
             to show auth options or forward authenticated users directly to main tabs. -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Setup wizard runs after registration/login when schedule baseline is missing. -->
        <activity
            android:name=".SetupScheduleActivity"
            android:exported="false" />

        <!-- Main screen remains non-exported to prevent external deep links from bypassing auth flow. -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Receiver is private to this app so only internally scheduled alarms can trigger it. -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

    </application>

</manifest>